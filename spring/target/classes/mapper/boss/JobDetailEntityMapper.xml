<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.david.hlp.Spring.boss.mapper.JobDetailEntityMapper">

    <resultMap id="BaseResultMap" type="com.david.hlp.Spring.boss.entity.JobDetailEntity">
        <id column="id" property="id"/>
        <result column="position_id" property="positionId"/>
        <result column="position_name" property="positionName"/>
        <result column="company_id" property="companyId"/>
        <result column="company_name" property="companyName"/>
        <result column="detail_data" property="detailData"/>
        <result column="gmt_create" property="gmtCreate"/>
        <result column="gmt_modified" property="gmtModified"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, position_id, position_name, company_id, company_name, detail_data, 
        gmt_create, gmt_modified, is_deleted
    </sql>

    <insert id="insert" parameterType="com.david.hlp.Spring.boss.entity.JobDetailEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_job_detail (
            position_id, position_name, company_id, company_name, detail_data
        ) VALUES (
            #{positionId}, #{positionName}, #{companyId}, #{companyName}, #{detailData}
        )
    </insert>

    <update id="updateById" parameterType="com.david.hlp.Spring.boss.entity.JobDetailEntity">
        UPDATE t_job_detail
        <set>
            <if test="positionName != null">
                position_name = #{positionName},
            </if>
            <if test="companyName != null">
                company_name = #{companyName},
            </if>
            <if test="detailData != null">
                detail_data = #{detailData},
            </if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE t_job_detail
        SET is_deleted = 1
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <select id="getById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_job_detail
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="getByPositionId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_job_detail
        WHERE position_id = #{positionId} AND is_deleted = 0
    </select>

    <select id="listByCompanyId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_job_detail
        WHERE company_id = #{companyId} AND is_deleted = 0
        ORDER BY gmt_create DESC
    </select>

    <select id="listByPositionName" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_job_detail
        WHERE position_name LIKE CONCAT('%', #{positionName}, '%') AND is_deleted = 0
        ORDER BY gmt_create DESC
    </select>

    <select id="listPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_job_detail
        WHERE is_deleted = 0
        ORDER BY gmt_create DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(1)
        FROM t_job_detail
        WHERE is_deleted = 0
    </select>
</mapper>
